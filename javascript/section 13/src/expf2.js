export const uname = 'Shail';

export function display(surname) {
    console.log(`Hello ${uname} ${surname}, how are you`);
};

const himessage = 'Hello this is default message';

export default himessage;